<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.charge.mapper.DiscountRuleMapper">

    <resultMap id="DiscountRuleResultMap" type="com.charge.entity.DiscountRule">
        <id property="id" column="id"/>
        <result property="ruleCode" column="rule_code"/>
        <result property="ruleName" column="rule_name"/>
        <result property="description" column="description"/>

        <result property="discountType" column="discount_type"/>
        <result property="discountValue" column="discount_value"/>
        <result property="maxDiscount" column="max_discount"/>

        <result property="status" column="status"/>
        <result property="effectiveStart" column="effective_start"/>
        <result property="effectiveEnd" column="effective_end"/>

        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO discount_rule (
            rule_code,
            rule_name,
            description,
            discount_type,
            discount_value,
            max_discount,
            status,
            effective_start,
            effective_end
        ) VALUES (
            #{ruleCode},
            #{ruleName},
            #{description},
            #{discountType},
            #{discountValue},
            #{maxDiscount},
            #{status},
            #{effectiveStart},
            #{effectiveEnd}
        )
    </insert>

    <update id="updateById">
        UPDATE discount_rule
        SET
            rule_name      = #{ruleName},
            description    = #{description},
            discount_type  = #{discountType},
            discount_value = #{discountValue},
            max_discount   = #{maxDiscount},
            status         = #{status},
            effective_start = #{effectiveStart},
            effective_end   = #{effectiveEnd}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="DiscountRuleResultMap">
        SELECT
            id,
            rule_code,
            rule_name,
            description,
            discount_type,
            discount_value,
            max_discount,
            status,
            effective_start,
            effective_end,
            create_time,
            update_time
        FROM discount_rule
        WHERE id = #{id}
    </select>

    <delete id="deleteById">
        DELETE FROM discount_rule
        WHERE id = #{id}
    </delete>

    <select id="selectList" resultMap="DiscountRuleResultMap">
        SELECT
            id,
            rule_code,
            rule_name,
            description,
            discount_type,
            discount_value,
            max_discount,
            status,
            effective_start,
            effective_end,
            create_time,
            update_time
        FROM discount_rule
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>